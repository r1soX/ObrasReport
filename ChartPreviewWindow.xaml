<Window x:Class="ObrasReport.ChartPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Предпросмотр графиков и диаграмм"
        Height="720" Width="980"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI" FontSize="13"
        Background="#F5F7FA">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="HeadingText" Text="Графики и диаграммы прогресса закрытия" FontSize="18" FontWeight="Bold" Foreground="#1F4E78"/>
            <TextBlock x:Name="SubtitleText" Foreground="#555" TextWrapping="Wrap" Margin="0,4,0,0"/>
            <TextBlock x:Name="WarningText" Foreground="#C62828" TextWrapping="Wrap" Margin="0,6,0,0"
                       Visibility="Collapsed"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#DDE3EA" BorderThickness="1" Background="White" CornerRadius="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="12">
                <ItemsControl x:Name="ChartsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,18">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#1F4E78" Margin="0,0,0,6"/>
                                <Image Source="{Binding Image}" Stretch="Uniform" MaxWidth="920"
                                       HorizontalAlignment="Left"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="#777" FontSize="12"
                       Text="После сохранения в Excel графики и диаграммы появятся на листе «Графики»."/>
            <Button Grid.Column="1" Content="Отмена" MinWidth="110" Padding="14,8" Margin="0,0,8,0"
                    Click="Cancel_Click" IsCancel="True"/>
            <Button Grid.Column="2" x:Name="SaveButton" Content="Сохранить в Excel…" MinWidth="160" Padding="14,8"
                    FontWeight="Bold" Background="#1F4E78" Foreground="White"
                    Click="Save_Click" IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
